# AI Sales Agent SaaS — Dependencies

# Core LLM & Agent Framework
langchain>=0.3.0
langchain-core>=0.3.0
langgraph>=0.2.0

# API server
fastapi>=0.110.0
uvicorn[standard]>=0.29.0
pydantic>=2.0.0

# HTTP client for webhooks
aiohttp>=3.9.0

# Session persistence (optional — falls back to in-memory)
redis>=5.0.0

# Environment management
python-dotenv>=1.0.0
sqlalchemy>=2.0.0
psycopg[binary]>=3.0

# Testing
pytest>=8.0.0
pytest-asyncio>=0.23.0
httpx>=0.27.0
